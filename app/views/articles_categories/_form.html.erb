<%= form_with(model: articles_category, local: true) do |form| %>
  <% if articles_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(articles_category.errors.count, "error") %> prohibited this articles_category from being saved:</h2>

      <ul>
      <% articles_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :article_id %>
    <%= form.number_field :article_id, id: :articles_category_article_id %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.number_field :category_id, id: :articles_category_category_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
