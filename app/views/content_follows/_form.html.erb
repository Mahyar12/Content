<%= form_with(model: content_follow, local: true) do |form| %>
  <% if content_follow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(content_follow.errors.count, "error") %> prohibited this content_follow from being saved:</h2>

      <ul>
      <% content_follow.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :article_id %>
    <%= form.number_field :article_id, id: :content_follow_article_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :content_follow_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
