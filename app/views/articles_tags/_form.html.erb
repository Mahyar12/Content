<%= form_with(model: articles_tag, local: true) do |form| %>
  <% if articles_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(articles_tag.errors.count, "error") %> prohibited this articles_tag from being saved:</h2>

      <ul>
      <% articles_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :article_id %>
    <%= form.number_field :article_id, id: :articles_tag_article_id %>
  </div>

  <div class="field">
    <%= form.label :tag_id %>
    <%= form.number_field :tag_id, id: :articles_tag_tag_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
